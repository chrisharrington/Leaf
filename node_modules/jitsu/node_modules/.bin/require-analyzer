#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../require-analyzer/bin/require-analyzer" "$@"
  ret=$?
else 
  node  "$basedir/../require-analyzer/bin/require-analyzer" "$@"
  ret=$?
fi
exit $ret
